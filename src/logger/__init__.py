"""
This module provides a customizable logging system with support for
color formatting and multiple handlers (e.g., console and file handlers).
It includes a Logger class that simplifies logger configuration and setup.
"""

from .logger import Logger
